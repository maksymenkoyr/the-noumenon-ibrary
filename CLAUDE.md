@AGENTS.md

## Documentation Standards

All project documentation lives in the `docs/` directory. When writing markdown, you MUST:

1. **Use ONLY standard, relative Markdown links** (e.g., `[Overview](./overview.md)`).
2. **NEVER use Obsidian wikilinks** (e.g., `[[Overview]]`).
